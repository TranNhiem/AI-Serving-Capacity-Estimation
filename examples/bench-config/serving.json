{
  "ascep_version": "0.1.0",
  "framework": "vllm",
  "framework_version": null,
  "framework_version_u_reason": "(U) not recorded at run time. This alone downgrades the report to partial: a different engine version can change KV pool size and scheduling behaviour, so these numbers are not strictly reproducible.",
  "container_digest": null,
  "container_digest_u_reason": "(U) not recorded at run time",
  "tensor_parallel": 2,
  "pipeline_parallel": 1,
  "gpu_count": 2,
  "max_model_len": 32768,
  "memory_utilization": null,
  "memory_utilization_u_reason": "(U) not recorded. Use ascep.capacity.calibrate_memory_utilization against the engine-reported KV token count to recover it before projecting to any other context length.",
  "batching_mode": "continuous",
  "prefix_caching": null,
  "prefix_caching_u_reason": "(U) not recorded. The most dangerous gap in this report: if it was on and the benchmark reused prompt prefixes, measured throughput overstates production.",
  "kv_cache_offload": null,
  "kv_cache_offload_u_reason": "(U) not recorded",
  "kv_cache_quantized": "off",
  "chunked_prefill": null,
  "chunked_prefill_u_reason": "(U) not recorded",
  "speculative_decoding": "off",
  "engine_reported_kv_cache_tokens": 1149595,
  "cold_start_to_ready_s": null,
  "cold_start_to_ready_s_u_reason": "(U) not timed",
  "image_input_transport": "n-a",
  "runtime_class": "server-batched",
  "context_overflow_policy": "refuse",
  "kv_cache_type_k": "n-a",
  "kv_cache_type_v": "n-a",
  "weights_residency": "resident",
  "runtime_build": null,
  "runtime_build_u_reason": "(U) no build identifier was recorded, and framework_version is null as well, so nothing in this declaration pins the code. Chapter 3's R11 holds this report to partial for that reason: the numbers below are not reproducible from the declaration alone.",
  "model_artifact_sha256": null,
  "model_artifact_sha256_u_reason": "(U) the checkpoint was served from a shared parallel filesystem by path, and no digest of the weights file was taken at run time. Consequence: the model declaration names the checkpoint but nothing here proves which bytes were loaded, so a rerun that finds a re-uploaded or re-quantised file at the same path would not be detectable from this report."
}
